spring.application.name=test-application


springdoc.swagger-ui.use-root-path=true


#spring.security.user.name=devuser
#spring.security.user.password=devpass
#logging.level.org.springframework.security=DEBUG
# You might also want to specify roles:
# spring.security.user.roles=USER


# PostgreSQL Datasource Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/testDB
spring.datasource.username=test
spring.datasource.password=test
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
# Other useful properties:
# spring.jpa.show-sql=true # To see the generated SQL queries in the console
# spring.jpa.properties.hibernate.format_sql=true # To format the SQL nicely

spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB